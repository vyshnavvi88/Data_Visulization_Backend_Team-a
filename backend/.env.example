# ================================================
# .env.example — Copy this file to .env and fill in your values
# ================================================

# MongoDB Atlas connection string (leave empty to use local Compass)
# MONGO_URI=mongodb+srv://<username>:<password>@trainsdata.exem1wb.mongodb.net/

# ML Model Version — change this when retraining the model
# IF_v1 = original model (anomaly_detection.py)
# IF_v2 = retrained model (retrain_pipeline.py) — current
MODEL_VERSION=IF_v2
